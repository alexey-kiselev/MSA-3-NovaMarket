http {
    upstream logistics_backend {
        server logistics.company.com max_fails=5 fail_timeout=30s;
    }

    server {
        listen 8080;

        location /logistics/ {
            proxy_pass http://logistics_backend;

            proxy_connect_timeout 3s;
            proxy_send_timeout 3s;
            proxy_read_timeout 3s;

            proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
            proxy_intercept_errors on;
            error_page 502 503 504 = @logistics_fallback;
        }

        location @logistics_fallback {
            default_type application/json;
            return 503 '{"error":"logistics_unavailable","message":"Delivery service temporarily unavailable"}';
        }
    }
}
